extends ../shared/template

block content
  h1= title

  form(action='/locations', method='post')
    input(type='text', name='name', placeholder='Location')
    input(type='number', name='rate', placeholder='Cost')
    button Save
